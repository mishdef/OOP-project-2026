<Window x:Class="PetrolWPF.View.UserEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit User" Height="399" Width="350" 
        WindowStartupLocation="CenterScreen" Background="#F4F4F4"
        ResizeMode="NoResize" Icon="/Accets/logo.jpg">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Full Name:"/>
        <TextBox x:Name="FullNameTextBox" Grid.Row="1" Height="30" VerticalContentAlignment="Center" Margin="0,0,0,10"/>

        <Label Grid.Row="2" Content="Username:" />
        <TextBox x:Name="UsernameTextBox" Grid.Row="3" Height="30" VerticalContentAlignment="Center" Margin="0,0,0,10"/>

        <Label Grid.Row="4" Content="Password:" />

        <Grid Grid.Row="5" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <PasswordBox x:Name="HiddenPasswordBox" 
                         Height="30" VerticalContentAlignment="Center" 
                         PasswordChanged="HiddenPasswordBox_PasswordChanged"/>

            <TextBox x:Name="VisiblePasswordTextBox" 
                     Height="30" VerticalContentAlignment="Center" 
                     Visibility="Collapsed" 
                     TextChanged="VisiblePasswordTextBox_TextChanged"/>

            <ToggleButton x:Name="ShowPasswordButton" 
                          Grid.Column="1" Width="30" Height="30" Margin="5,0,0,0" 
                          Content="👁" FontSize="12"
                          Click="ShowPasswordButton_Click">
                <ToggleButton.Template>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}" BorderBrush="LightGray" BorderThickness="1" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </ToggleButton.Template>
            </ToggleButton>
        </Grid>

        <Label Grid.Row="6" Content="Role:"/>
        <ComboBox x:Name="RoleComboBox" Grid.Row="7" Height="30" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="Save" Width="80" Height="35"  FontWeight="Bold" Margin="0,0,10,0" Click="SaveButton_Click"/>
            <Button Content="Cancel" Width="80" Height="35"  Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>